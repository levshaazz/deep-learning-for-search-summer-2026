{
  "name": "deep-learning-for-search-summer-2026",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Course website (two modes: scrollytelling Book + reference pages) for Deep Learning for Search, Innopolis University, Summer 2026.",
  "scripts": {
    "dev": "astro dev",
    "build": "node scripts/build-deck-layout.mjs && node scripts/build-deck-widgets.mjs && node scripts/assemble-chapter.mjs build && astro build && node scripts/assemble-deck.mjs build && node scripts/copy-static.mjs && node scripts/optimize-images.mjs",
    "preview": "astro preview",
    "astro": "astro",
    "runs": "python3 scripts/extract_runs.py && python3 _research/check_notebooks.py",
    "runs:watch": "python3 scripts/watch_runs.py",
    "runs:prepare": "python3 scripts/sync_solved.py",
    "hooks:install": "python3 scripts/install_hooks.py"
  },
  "dependencies": {
    "astro": "^5.6.0",
    "scrollama": "^3.2.0"
  },
  "devDependencies": {
    "sharp": "^0.35.2"
  }
}
