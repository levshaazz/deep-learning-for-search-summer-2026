<!-- beats: hook-first-contact, problem-raw-text, turn-tokenize, climb-bpe, stats-zipf-heaps, problem-length-lies, turn-measure-angle, climb-cosine, catch-curse-highd, payoff-knights-win -->
<!-- book: 02 -->

# L2.md — *First Contact* (beat sheet) — the canonical contract

> **Lecture:** NLP · Tokenization · Similarity. **Mode:** sci-fi → LOTR. **Spine:** Get Data → Measure.
> **Creatures:** **Tokenosaurus**, **Sir Cosine & the Knights of the Unit Sphere**, **Curse-of-
> Dimensionality Wraith**. **Slides:** `/slides/02` (EN, exists, 71). **Book:** `/{lang}/book/02`.
> **Open:** *"There's an alien on my screen and I can't tell if two things it says mean the same."*
> **Close:** the machine now *measures meaning*; bridge to embeddings (L4). Worked end-to-end in
> [REFERENCE_IMPL_L2.md](../_research/iterations/REFERENCE_IMPL_L2.md) — this file is the canonical contract.

## Spine position
Two territories in one mission: **Get Data** (turn alien text into computable units) → **Measure**
(tell when two meanings are close). Builds the tokenizer (Archives) and the first Instrument (cosine).

## Beats
| # | beat.id | kind | precision | widget | Slide (EN, terse) | Book (3-lang prose) |
|---|---------|------|-----------|--------|-------------------|---------------------|
| 0 | `hook-first-contact` | hook | — | — | `divider`: Serega-at-console + alien | Cold open: subtitles are gibberish; how to tell two phrases *mean* the same |
| 1 | `problem-raw-text` | problem | — | — | `viz`: raw text isn't computable; OOV words | The naive move (exact words) breaks on unseen/variant forms |
| 2 | `turn-tokenize` | turn | — | — | `divider`: split into sub-words | Enter **Tokenosaurus** — snips words into chunks |
| 3 | `climb-bpe` | **climb** | **true** | **bpe-merge-ledger** | `walkthrough`: BPE/WordPiece merges (slides 29–31) | The merge ledger, exact scores; why sub-words beat whole-words on OOV |
| 4 | `stats-zipf-heaps` | stakes | — | `zipf-heaps` | `viz`: Zipf slope≈-1.02; Heaps β≈0.59, R²≈0.998, V=94287 | Zoom: vocabulary growth; the long tail (the data is *shaped*) |
| 5 | `problem-length-lies` | problem | — | — | `quote`/`viz`: "a longer sentence isn't more meaning" | Raw distance is fooled by magnitude — *length lies* |
| 6 | `turn-measure-angle` | turn | — | — | `divider`: measure the **angle**, not the length | Enter **Sir Cosine & the Knights of the Unit Sphere** |
| 7 | `climb-cosine` | **climb** | **true** | **cosine-sphere** | `e2e`/`formula`: cos, ‖x−y‖²=2(1−cosθ), √162≈12.73 | The worked example, exact, clean — no jokes |
| 8 | `catch-curse-highd` | catch | — | `highd-histogram` | `misconception`/`quiz`: high-d distances concentrate | Enter the **Wraith** — flattens the histogram to a spike |
| 9 | `payoff-knights-win` | payoff | — | — | `final` callback | Knights hold the sphere; First Contact resolved; bridge → embeddings (L4) |

## Widgets & data
- `bpe-merge-ledger` ← `data/l2-bpe.json`; `zipf-heaps` ← `data/l2-corpus-stats.json`;
  `cosine-sphere` ← `data/l2-cosine.json` (cos, euclid≈12.73, relation); `highd-histogram` ← `data/l2-highd.json`.

## Creatures & callbacks
- **Tokenosaurus** → cameo at L9 (chunking). **Sir Cosine** → rides L4–L7, L11 (CLIP). **Wraith** →
  returns L8 (why ANN/PQ needed). Refs: Sennrich 2016, Schuster/Nakajima 2012, Ethayarajh, Aggarwal/Beyer (high-d).

## Acceptance (beat-coverage)
Both climbs (BPE + cosine) exact; the three creatures used only on hook/catch beats; catchphrase opens
+ closes; matches REFERENCE_IMPL_L2 b.3.
