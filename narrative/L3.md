# L3.md — *The Star Catalog* (beat sheet)

> **Lecture:** Classical IR (TF-IDF, BM25, inverted index) · Ranking Metrics. **Mode:** sci-fi.
> **Spine:** Measure + Rank. **Creatures:** **Goodhart** (return), **Lexical Gremlin** (cameo — BM25's
> blind spot). **Slides:** `/slides/03` (EN, to build). **Book:** `/{lang}/book/03`.
> **Open:** *"You can't fly past every star to find one. You need a catalog."*
> **Close:** the catalog finds fast — but it's blind to synonyms (the Gremlin still lurks → L4).

## Spine position
Builds the **inverted index** (Archives → fast lookup) and the **classical scorer + metrics**
(Instruments). The first end-to-end *findable-fast* Ship.

## Beats
| # | beat.id | kind | precision | widget | Slide (EN, terse) | Book (3-lang prose) |
|---|---------|------|-----------|--------|-------------------|---------------------|
| 0 | `hook-catalog` | hook | — | — | `divider`: the star catalog | Cold open: linear scan is hopeless; build a catalog |
| 1 | `problem-linear-scan` | problem | — | — | `viz`: scan N docs per query = death | Why brute force can't serve a query in time |
| 2 | `turn-inverted-index` | turn | — | — | `divider`: term → postings list | The inverted index reveal (the catalog) |
| 3 | `climb-index` | **climb** | **true** | **inverted-index** | `walkthrough`: build postings; term→docIDs | Index structure, exact; query = list intersection |
| 4 | `turn-scoring` | turn | — | — | `divider`: how to *score*? TF-IDF → BM25 | The trusty sextant: term weighting |
| 5 | `climb-bm25` | **climb** | **true** | **bm25-calc** | `e2e`/`formula`: BM25 with k₁, b, idf; worked tf saturation | BM25 worked example, exact; why saturation + length-norm |
| 6 | `turn-metrics` | turn | — | — | `divider`: how do we know it's *good*? | The pivot to evaluation |
| 7 | `climb-metrics` | **climb** | **true** | **metrics-ledger** | `walkthrough`: P@k, MAP, MRR, nDCG worked | Each metric defined + computed, exact; what each rewards |
| 8 | `catch-goodhart` | catch | — | — | `misconception`: optimizing the metric ≠ relevance | **Goodhart returns** — the metric becomes a target |
| 9 | `payoff-blind-catalog` | payoff | — | — | `final` callback | Fast but synonym-blind; bridge → give words *meaning* (L4) |

## Widgets & data
- `inverted-index` ← `data/l3-index.json`; `bm25-calc` ← `data/l3-bm25.json` (tf, df, N, k₁, b, idf, score);
  `metrics-ledger` ← `data/l3-metrics.json` (a ranked list + gains → P@k/MAP/MRR/nDCG).

## Creatures & callbacks
- **Goodhart** (from L1) returns as the *Measure*-territory recurring villain → L10. **Lexical Gremlin**
  cameo: BM25 can't bridge "couch"/"sofa" → motivates embeddings (L4) and its defeat (L5).

## Acceptance (beat-coverage)
Three climbs (index, BM25, metrics) exact; Goodhart reused (not duplicated); catchphrase opens+closes;
bridge to L4 explicit.
