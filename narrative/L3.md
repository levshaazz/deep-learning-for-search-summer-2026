<!-- beats: hook-catalog, problem-linear-scan, depth-the-slowest-shard-sets-the-clock, turn-inverted-index, climb-index, turn-index-internals, climb-compression, depth-why-skips-are-spaced-by-a-square-root, turn-boolean-phrase, depth-rarest-first-is-a-bound, depth-what-positions-cost, turn-scoring, turn-bow, depth-bow-limits, depth-the-bags-other-customer, depth-vector-space-model, depth-raw-counts-fail, climb-tfidf, depth-tf-variants, depth-idf-smoothing, depth-cosine-ranker, turn-two-weightings-one-formula, depth-keyword-extraction, catch-rare-here-is-not-about, depth-near-duplicates, depth-why-tf-and-idf-multiply, climb-bm25, climb-bm25-numbers, climb-bm25-history, depth-prp, depth-per-term-sum, depth-the-query-side-of-saturation, depth-one-mention-is-the-bare-idf, turn-the-correction-they-switched-off, depth-eliteness, depth-two-dials, turn-read-a-benchmark-twice, turn-wand, depth-the-bound-that-saturation-buys, turn-fusion, depth-scores-that-cannot-be-compared, turn-fusion-family, climb-rrf, depth-hybrid-fusion, depth-the-two-limits-of-the-shift, depth-the-swap-fusion-refuses-to-settle, turn-pagerank, depth-two-holes-in-the-walk, turn-pagerank-power, depth-pagerank-feature, depth-the-vote-that-splits, depth-why-the-walk-settles, catch-gremlin, payoff-catalog -->
<!-- book: 03 -->

# L3.md — *The Star Catalog* (beat sheet + build design)

> **Lecture:** Classical IR — TF-IDF, BM25, inverted index · Full-text Search Ranking & **Rank Fusion**.
> **Mode:** sci-fi. **Spine:** Measure + Rank. **Creatures:** **Lexical Gremlin** (cameo — BM25's
> blind spot, sets up embeddings). **Slides:** `/slides/03` (EN, to build). **Book:** `/{lang}/book/03`.
> **Open:** *"You can't fly past every star to find one. You need a catalog."*
> **Close:** the catalog finds *fast* — but how do we know it finds *well*? (→ L5 Metrics) and it's
> blind to synonyms (→ L6 embeddings). Built per [NARRATIVE_METHOD.md](../_research/iterations/NARRATIVE_METHOD.md)
> + [REFERENCE_IMPL_L2.md](../_research/iterations/REFERENCE_IMPL_L2.md). **Rank Fusion moved here from
> the old L10 'Alliance'** (now L12) per the L3/L4 split.

## Spine position
Builds the **inverted index** (Archives → instant lookup) and the **classical scorer + fusion**
(Instruments/Bridge). The first end-to-end *findable-fast* Ship — the baseline every neural method
must beat.

## Beats
> **Deepened (Summer 2026):** five new **prose** beats added in lockstep with `content/book/l3.js`
> (`turn-index-internals`, `turn-bow`, `climb-bm25-history`, `turn-fusion-family`, `turn-pagerank`).
> They narrate PLAN §3/§5 deep content (index construction/compression/skip-merge/boolean;
> Bag-of-Words + vector space + spam filter; the BM1→BM25 engineering saga; the fusion family;
> PageRank/power iteration). All scrolly beats keep their existing widget + data keys.
>
> **Book parity backfill (PLAN §2-Book / §4):** one more **prose** beat — `climb-bm25-numbers`,
> right after the `climb-bm25` widget — walks the BM25 mechanism **on the cat/dog numbers in prose**
> (smoothed idf(cat)=0.1335, idf(dog)=0.4700; the saturating B-factor with one worked term D2-dog
> B=1.2903→weight 0.6065; per-doc sums D2=0.7226 > D1=0.6608 > D3=0.1571), so a Book-only reader sees
> the numbers move, not just the saga. `turn-pagerank` now names the worked authority B≈0.40 concretely.
> All numbers sourced from `data/l3-bm25-catdog.json` + `…-steps.json` + `data/l3-pagerank.json`.
>
> **Deck-parity backfill (Summer 2026, gap audit P2):** two more **prose** beats close the last
> deck-vs-Book holes flagged in `_internal/book_audit2/p2-gaps.md` (L3 rows). `turn-boolean-phrase`
> (after `climb-compression`) works the **boolean model** (AND=∩, OR=∪, NOT=∖ on the cat/dog/mouse
> postings) and the **positional/phrase index** (`new→[3,11]`/`york→[4,27]`; phrase = positions gap 1,
> proximity = gap ≤ k). `turn-wand` (after `climb-bm25-history`) adds **WAND / BlockMax-WAND** — the
> "how does BM25 actually serve under the latency budget" payoff that closes the L1 loop: per-term
> max-score upper bounds vs a running k-th-best threshold skip docs that can't reach the top-k, and
> per-block maxima make top-k effectively sublinear; it also names the BM25F/BM25+ variants. Both are
> self-contained worked examples (numbers from the deck slides 19–20 and 55); no new data files. Inline
> citations added throughout to the real `data/papers.json` URLs (Spärck Jones, RSJ/Robertson & Spärck
> Jones, Robertson 2-Poisson, Robertson & Walker BM25, Robertson & Zaragoza, Salton VSM, Cormack RRF,
> Page-Brin PageRank, Thakur BEIR, Manning et al. IIR). No scrolly beat or widget/data key changed.

| # | beat.id | kind | precision | widget | Slide (EN, terse) | Book (3-lang prose) |
|---|---------|------|-----------|--------|-------------------|---------------------|
| 0 | `hook-catalog` | hook | — | — | `divider`: the star catalog | Cold open: one record in a billion; you can't read them all at query time |
| 1 | `problem-linear-scan` | problem | — | — | `viz`: scan N docs per query = death | Why brute force (score every doc) can't serve a query in time |
| 2 | `turn-inverted-index` | turn | — | — | `divider`: term → postings list | The inverted index reveal (the catalog: word → which docs) |
| 3 | `climb-index` | **climb** | **true** | **inverted-index** | `walkthrough`: build postings; AND/OR query = list intersection | Index build on a tiny real corpus, exact; query = merge postings |
| 4 | `turn-index-internals` *(new)* | turn | — | — | `divider`/`viz`: anatomy of a posting; build/compress/skip-merge | Inside a catalog card: postings (tf, positions), BSBI/SPIMI build + Lucene segments, gap+varbyte compression, skip-pointer merge, boolean (rarest-first) |
| 5 | `climb-compression` *(new)* | **climb** | **true** | **postings-compression** | `walkthrough`: docIDs → gaps → varbyte; 16B→4B | **Watch gap-encoding shrink a postings list** — raw IDs 3,8,12,30 (16 B naive) → gaps 3,+5,+4,+18 → 1-byte varbyte each (7 data bits + continuation) → 4 B total, clean 4× (numbers from `data/l3-compression.json`) |
| 6 | `turn-boolean-phrase` *(new)* | turn | — | — | `walkthrough`: AND/OR/NOT = ∩/∪/∖; positional phrase merge | **Boolean model + positional/phrase index** — AND=∩ (`cat AND dog`=[D1,D2]), OR=∪ (`dog OR mouse`=[D1,D2,D3]), NOT=∖ (`cat AND NOT mouse`=[D1]); rarest-term-first; positional index `new→[3,11]`/`york→[4,27]`, phrase = positions gap 1 (3→4 ✓, 11→27 ✗), proximity = gap ≤ k (Manning et al. 2008) |
| 7 | `turn-scoring` | turn | — | — | `divider`: ranking, not just matching — TF-IDF → BM25 | We have *which* docs; now *order* them — matching is binary, relevance is a spectrum |
| 8 | `turn-bow` *(new)* | turn | — | — | `divider`/`e2e`: Bag-of-Words + document-term matrix + spam filter | **Bag-of-Words named**; vector space model (Salton et al. 1975); live spam-filter example; the order-is-lost catch |
| 9 | `climb-tfidf` | **climb** | **true** | **bm25-calc** *(tf-idf mode)* | `e2e`: tf · idf; why rare terms weigh more | TF-IDF worked example, exact; idf intuition (Spärck Jones 1972); car/truck (idf 0 → common words vanish); the two flaws |
| 10 | `climb-bm25` | **climb** | **true** | **bm25-calc** | `e2e`: BM25 with k₁, b; tf saturation + length norm | **BM25 worked example, exact** — the headline; why saturation + length-norm fix TF-IDF |
| 11 | `climb-bm25-numbers` *(new)* | climb | — | — | `e2e`/prose: BM25 mechanism on cat/dog numbers | **BM25 numbers in prose (parity)** — smoothed idf(cat)=0.1335, idf(dog)=0.4700; saturating B-factor, worked term D2-dog B=1.2903→0.6065; per-doc sums D2=0.7226 > D1=0.6608 > D3=0.1571 |
| 12 | `climb-bm25-history` *(new)* | turn | — | — | `timeline`: BM1→BM11→BM15→BM25; Robertson/SJ/Okapi/TREC; 2-Poisson | **The BM25 saga** — each refinement motivated by the prior's failure; RSJ idf (Robertson & Spärck Jones 1976), 2-Poisson "why" of saturation (Robertson 1981), b as a dial BM11↔BM15, Okapi/TREC provenance (Robertson & Walker 1994), BEIR ≈0.43 baseline (Thakur et al. 2021) |
| 13 | `turn-wand` *(new)* | turn | — | — | `viz`: top-k with score upper bounds; WAND/BlockMax-WAND | **How BM25 serves fast** — only the top-k matters; WAND = per-term max-score upper bound vs running k-th-best threshold → skip docs that can't beat it; BlockMax-WAND = per-block maxima → leap whole blocks, effectively sublinear top-k; closes L1 latency loop; BM25F/BM25+ named (Robertson & Zaragoza 2009) |
| 14 | `turn-fusion` | turn | — | — | `divider`: one ranker isn't enough — fuse them | Different rankers (BM25 vs TF-IDF cosine) see different things; incomparable scales |
| 15 | `turn-fusion-family` *(new)* | turn | — | — | `divider`/`table`: CombSUM/MNZ · Borda · CPMF → RRF | The fusion family before RRF: score-normalisation (CombSUM/MNZ) vs rank-only (Borda/CPMF); why a gently-discounted rank-only method wins (Cormack et al. 2009) |
| 16 | `climb-rrf` | **climb** | **true** | **rrf-fusion** | `walkthrough`: RRF = Σ 1/(k+rankᵢ); fuse 2 lists | RRF worked example, exact; why rank-based fusion beats score-mixing |
| 17 | `turn-pagerank` *(new)* | turn | — | — | `divider`/`viz`: link = vote; power iteration | PageRank: link-voting (Page, Brin et al. 1998), random surfer + teleport (d=0.85), power iteration on a 3-page web (B the authority, ≈0.40); query-independent vs query-dependent signal |
| 17b | `turn-pagerank-power` *(new)* | **climb** | **true** | **pagerank-power** | `e2e`: power iteration on a 3-page web (5 steps) | **Watch the rank vector converge** — 3-page graph A→B,B→C,C→A,C→B (B = authority, 2 in-links); uniform start 1/3, worked B-update base 0.05 + 0.85·0.5 = PR₁(B)=**0.475**, bars settle through v₅=(0.1993,0.4058,0.3949) to converged **(0.2148, 0.3974, 0.3878)**; B 0.3974 > C 0.3878 > A 0.2148, the query-independent signal (numbers from `data/l3-pagerank.json`, d=0.85) |
| 18 | `catch-gremlin` | catch | — | — | `misconception`: BM25 can't bridge "couch"/"sofa" | The **Lexical Gremlin** cameo — exact-term scoring is synonym-blind (sets up L6) |
| 19 | `payoff-catalog` | payoff | — | — | `final` callback | Fast catalog built; bridge → *is it any good?* (L5 metrics) + *give words meaning* (L6) |

## Worked examples (precision beats — пошаговые, exact)
All numbers are **computed by `_research/gen_l3.py`** on a tiny **20 Newsgroups** sub-corpus (the
dataset L1/L2 already use), so they're real and reproducible, and a future facts-gate claim can pin them.

1. **Inverted index (climb-index).** Take ~8 short docs across 2 categories (e.g. `sci.space`,
   `rec.sport.hockey`). Show the postings for 3 query terms (`space`, `orbit`, `team`): `term → [docIDs]`.
   A 2-term AND query = intersection of two postings lists (show the merge step by step).
2. **TF-IDF (climb-tfidf).** For query `space orbit` vs a chosen doc d:
   - `tf(t,d)` (raw count), `df(t)`, `N` (docs), `idf(t) = ln((N − df + 0.5)/(df + 0.5) + 1)`.
   - weight = `tf · idf`; sum over query terms. Show that `orbit` (rarer) contributes more than `space`.
3. **BM25 (climb-bm25) — the headline.** Same query/doc, `k₁ = 1.5`, `b = 0.75`:
   `BM25(q,d) = Σ_t idf(t) · ( tf · (k₁+1) ) / ( tf + k₁·(1 − b + b·|d|/avgdl) )`.
   Show, step by step: (a) idf per term; (b) the saturation factor (going from tf=1→2→5 adds *less*
   each time — contrast with linear TF-IDF); (c) the length-norm `|d|/avgdl` penalising a long doc.
   End on the final per-doc score and the ranking of 3 candidate docs.
4. **RRF (climb-rrf).** Two rankers for one query — BM25 vs TF-IDF-cosine — give two ranked lists.
   `RRF(d) = Σ_rankers 1/(k + rank_r(d))`, `k = 60`. Show the table: each doc's rank in each list,
   its reciprocal-rank contributions, the summed RRF score, and the fused order. Point: a doc ranked
   well by *both* rises; rank-based fusion is robust to incomparable score scales.

## Python / data plan
`_research/gen_l3.py` (seeded, stdlib + sklearn + numpy; same family as `gen_highd.py`):
- load 20NG, select a small deterministic sub-corpus; tokenize (reuse the L2 tokenizer notion).
- build the inverted index → `data/l3-index.json` (`{term: [docIDs], df}` + the AND-merge trace).
- compute TF-IDF and BM25 (`k₁=1.5, b=0.75`) for a fixed query → `data/l3-bm25.json`
  (`{N, avgdl, query, docs:[{id, len, terms:[{t, tf, df, idf, tfidf, bm25}], tfidfScore, bm25Score}]}`).
- produce two ranked lists + the RRF fusion (`k=60`) → `data/l3-rrf.json`
  (`{lists:{bm25:[...], cosine:[...]}, fused:[{id, rrf, contributions}]}`).
- **Relevance for the narrative** uses the L1/L2 convention: *relevant = same 20NG category* (this
  is what L5 metrics will consume — keep the qrels here too for continuity).
- Lab tie-in (Fri session): students reimplement BM25 from the postings and reproduce the ranking.

## Widgets (build design — same contract as REFERENCE_IMPL_L2 a.6)
- `inverted-index` ← `data/l3-index.json`: reveal postings per term; final step animates an AND-merge.
- `postings-compression` ← `data/l3-compression.json`: stepped — raw 32-bit doc IDs → gaps (Δ) →
  variable-byte bytes (1 continuation + 7 data bits) → totals bar (16 B → 4 B, 4×). Drives `climb-compression`.
- `bm25-calc` ← `data/l3-bm25.json`: stepped — idf → tf-saturation → length-norm → final score; a
  `tf-idf mode` flag reuses it for climb-tfidf (so one widget serves beats 5 & 6).
- `rrf-fusion` ← `data/l3-rrf.json`: two columns of ranked lists → arrows → fused column; reveal the
  reciprocal-rank sum per doc.

## Image prompts (mascot) → see `_research/iterations/IMAGE_PROMPTS_L3_L4.md`
Serega the **star-cartographer** plotting an inverted index as a constellation catalog (hook/divider);
BM25 as a trusty brass **sextant**; the Lexical Gremlin wedging a wall between "couch" and "sofa" (catch).

## Creatures & callbacks
- **Lexical Gremlin** (from L1) cameo — BM25's synonym blindness motivates embeddings (L6) and its
  defeat (L7). Callback: the L1 retrieve→rank funnel — L0 retrieval is exactly BM25/the index here.
  (Goodhart now lives in **L5**, the metrics lecture.)

## Acceptance (beat-coverage + facts-gate-ready)
Four precision climbs (index, TF-IDF, BM25, RRF) computed by `gen_l3.py` and shown exact; Rank Fusion
present (moved from old Alliance); Gremlin only on the catch beat; catchphrase opens + closes; bridges
to L5 (metrics) and L6 (embeddings) explicit.
