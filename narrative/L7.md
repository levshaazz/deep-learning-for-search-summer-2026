<!-- beats: hook-council, problem-one-vector, turn-contextual, depth-context-derivative, turn-attention, depth-blend-hull, climb-attention, climb-attention-geo, depth-shapes-and-einsum, depth-cost-wall, turn-selfattention, depth-qk-asymmetry, depth-score-variance, depth-masks-families, climb-multihead, depth-heads-learn, turn-positional, depth-add-not-concat, climb-positional, depth-modern-pe, turn-block, climb-block, climb-block-geo, climb-layernorm, depth-layernorm-geometry, depth-block-modern, depth-residual-viz, turn-contextual-payoff, depth-what-depth-buys, depth-pooling, turn-contrastive, depth-triplet-to-infonce, climb-contrastive, depth-hardneg, depth-batch-is-the-exam, depth-matryoshka, catch-anisotropy, depth-cone-split, depth-why-the-norm-is-dropped, depth-numerical, crossdomain-vision, crossdomain-everything, depth-cage-mechanism, payoff-gremlin-caged -->
<!-- book: 07 -->

# L6.md — *The Council of Attention* (beat sheet + build design)

> **Lecture:** Dense & contextual embeddings · contrastive learning · Transformers & Attention.
> **Mode:** sci-fi. **Spine:** Get Data → Measure. **Creatures:** **Lexical Gremlin** (DEFEATED — the payoff),
> **Sir Cosine** (the contrastive objective), **the Council / Attention-Head** (NEW — each token a councillor
> deciding who to heed). **Slides:** `/slides/07` (EN, to build, A+ depth). **Book:** `/{lang}/book/07`.
> **Open:** *"The crew has too many voices. Who do you listen to?"*
> **Close:** synonyms now collapse together — **the Gremlin is finally caged** (big callback to L1). Bridge → bi/cross-encoders (L10).

## Spine position
Upgrades the meaning-map from **static to contextual** (Archives) and learns a representation **for search**
(Measure). The **Transformer is the engine**; **attention** is how the crew decides who to heed; **contrastive
learning** shapes the space so retrieval works. Full transformer block taught (per decision), kept anchored to
"why this matters for search."

## Decisions locked
- Depth **A+** (full deck ~50–65 slides + deep trilingual Book chapter, climbs exact).
- **Full transformer block** (positional encoding, FFN, residual + LayerNorm, layer stack) — not just attention.
- New minor creature **the Council / Attention-Head** (each token attends — votes on who to heed).
- Built **after L6**.

## Beats (A+ — 27: 16 core + 11 Book-only depth/cross-domain deepenings)

> The 11 `depth-*` / `crossdomain-*` beats are **Book-only prose** (deck unchanged): concept-depth,
> corner-cases, and cross-domain "did-you-know" asides added to the chapter. They carry no widget and
> no slide; the slide column reads `—` (Book-deepen). The 16 original beats are untouched.

| # | beat.id | kind | precision | widget | Slide (EN, terse) | Book (3-lang prose) |
|---|---------|------|-----------|--------|-------------------|---------------------|
| 0 | `hook-council` | hook | — | — | `divider`: the Council of Attention | Cold open: a word's meaning depends on its neighbors — too many voices, who to heed? |
| 1 | `problem-one-vector` | problem | — | — | `viz`: "bank" (river vs money) — one static vector | Static embeddings (L6) can't be in two minds; context is missing |
| 2 | `turn-contextual` | turn | — | — | `divider`: a vector *per context*, not per word | Contextual embeddings reveal: the same word, different coordinates by sentence |
| 3 | `turn-attention` | turn | — | — | `divider`: let each token *attend* to the others | The attention idea: build a token's meaning from a weighted blend of its neighbors |
| 4 | `climb-attention` | **climb** | **true** | **ncd-chain** | `e2e`: \(\mathrm{softmax}(QK^\top/\sqrt{d_k})V\), synced numeric example | Scaled dot-product attention, exact; Q/K/V roles; why divide by \(\sqrt{d_k}\) |
| — | `climb-attention-geo` | **scrolly** | **true** | **attention-geometry** | — (book-only) viz | The geometry of attention: the query is pulled to the attention-weighted average of the value points — the same output row as the numbers (blended4d ≈ output[1]) |
| 5 | `depth-cost-wall` | book-deepen | — | — | — (Book-only) | Depth: attention is \(O(n^2)\) — the cost wall; long-context (sparse/linear, FlashAttention, exact math) |
| 6 | `turn-selfattention` | turn | — | — | `divider`: Q, K, V all from the SAME sequence | Self-attention: every token queries every token (incl. itself) |
| 7 | `depth-masks-families` | book-deepen | — | — | — (Book-only) | Depth: causal (decoder) vs bidirectional (encoder) masks; encoder/decoder/enc-dec families + KV-cache; search uses encoders |
| 8 | `climb-multihead` | **climb** | **true** | **ncd-chain** *(multi-head view)* | `e2e`/`viz`: h heads in parallel subspaces → concat → project | Multi-head: parallel attention "councillors", each a different relation; concat + \(W^O\) |
| 9 | `depth-heads-learn` | book-deepen | — | — | — (Book-only) | Depth: what heads actually learn (positional/syntactic/rare-token; pruning); attention is permutation-equivariant |
| 10 | `turn-positional` | turn | — | — | `divider`: attention is order-blind → inject position | Permutation-invariance problem: "dog bites man" = "man bites dog" without position |
| 11 | `climb-positional` | **climb** | **true** | **positional-enc** | `formula`/`viz`: sinusoidal PE \(\sin/\cos(pos/10000^{2i/d})\) | Positional encoding, exact: the sinusoid grid; learned vs fixed |
| 12 | `depth-modern-pe` | book-deepen | — | — | — (Book-only) | Depth: RoPE (rotation → relative offset) / ALiBi vs sinusoidal absolute PE; extrapolation to long context |
| 13 | `turn-block` | turn | — | — | `divider`: assemble the Transformer block | The block reveal: attention is one piece of a repeating unit |
| 14 | `climb-block` | **climb** | **true** | **transformer-block** *(archflow in deck)* | `archflow`: multi-head attn → Add&Norm → FFN → Add&Norm; stack ×N | The full encoder block, exact: residual + LayerNorm (why), position-wise FFN, depth = stacking |
| — | `climb-block-geo` | **scrolly** | **true** | **block-geometry** | — (book-only) viz | The block as geometry: each sublayer reshapes the token cloud (attention contracts → Add&Norm rescales to a unit ring → FFN warps each point → Add&Norm) |
| — | `climb-layernorm` | **scrolly** | **true** | **layernorm-viz** | — (book-only) viz | What LayerNorm DOES: recenter (mean 5→0) → unit-scale (var 7.5→1, onto the sphere) → learned γ·+β |
| 15 | `depth-block-modern` | book-deepen | — | — | — (Book-only) | Depth: pre-norm vs post-norm (training stability); residual highway; FFN-as-key-value-memory aside |
| — | `depth-residual-viz` | **scrolly** | **true** | **residual-stream** | — (book-only) viz | The residual highway: x → x+attn(x) → Norm → x+ffn(x) → Norm; both LayerNorm stages renorm to √6 ≈ 2.4495 |
| 16 | `turn-contextual-payoff` | turn | — | — | `divider`/`viz`: stack → contextual token vectors → a sentence vector | BERT-style: the stack yields contextual vectors; `[CLS]`/mean-pooling → one vector for a sentence/doc |
| 17 | `depth-pooling` | book-deepen | — | — | — (Book-only) | Depth/corner: `[CLS]` vs mean vs last pooling — which for retrieval (ties L10); padding-mask leakage + attention-sink |
| 18 | `turn-contrastive` | turn | — | — | `divider`: but train the space *for retrieval* | Off-the-shelf vectors aren't tuned for search — pull positives together, push negatives apart |
| 19 | `climb-contrastive` | **climb** | **true** | **contrastive-space** | `e2e`/`formula`: InfoNCE / triplet; in-batch negatives | The contrastive objective, exact; cosine in the loss; in-batch negatives are free positives' foils |
| 20 | `depth-hardneg` | book-deepen | — | — | — (Book-only) | Depth: hard-negative mining + the false-negative trap; alignment–uniformity + collapse; why τ/normalize |
| 21 | `depth-matryoshka` | book-deepen | — | — | — (Book-only) | Depth: Matryoshka (truncatable dims) + distillation (SBERT from a cross-encoder) — ties L10 |
| 22 | `catch-anisotropy` | catch | — | — | `misconception`/callback L2: raw contextual vectors are anisotropic | The trap (P7): un-tuned BERT vectors crowd a narrow cone (callback L2 anisotropy) — why contrastive + normalize |
| 23 | `depth-numerical` | book-deepen | — | — | — (Book-only) | Corner: softmax max-subtraction (overflow), LayerNorm ε; the anisotropy frequency mechanism + whitening fix |
| 24 | `crossdomain-vision` | book-deepen | — | — | — (Book-only) | Cross-domain: ViT (patches as tokens, SAME encoder block); CLIP (InfoNCE in a shared image↔text space) — ties L20 |
| 25 | `crossdomain-everything` | book-deepen | — | — | — (Book-only) | Cross-domain: Transformer = GNN on a complete graph + GAT/wav2vec/protein-LM/DNABERT/Enformer → "everything is a sequence of tokens" |
| 26 | `payoff-gremlin-caged` | payoff | — | — | `final` callback (the big one) | **Lexical Gremlin CAGED** (callback L1): "couch" and "sofa" now collapse together; bridge → bi/cross-encoders (L10) |

## Widgets & data (NEW)
- **`ncd-chain`** ← `data/l6-attention.json` — a small worked self-attention: tokens, \(Q,K,V\) matrices,
  \(QK^\top\) scores, \(/\sqrt{d_k}\), softmax weights, weighted-sum output; a 2-head variant. Steps:
  Q·Kᵀ → scale → softmax (attention heatmap) → ·V → output. (Can reuse the deck's existing `e2e` attention slide.)
- **`positional-enc`** ← `data/l6-positional.json` — the sinusoidal PE grid (pos × dim values); a viz/heatmap.
- **`transformer-block`** — the full encoder block as a deck **`archflow`** (attn → Add&Norm → FFN → Add&Norm,
  ×N); a stepped Book scrolly if cheap, else a static labeled figure.
- **`contrastive-space`** ← `data/l6-contrastive.json` — anchor/positive/negative vectors, their cosines, the
  loss; steps: scatter → pull positive in / push negatives out → loss drops. Sir Cosine powers the metric.
- Data gen: `_research/gen_l6.py` (reproducible; reuse the embedding pattern). Attention numbers can be a
  fixed small matrix (deterministic); contrastive over real L6 vectors; PE is closed-form. Facts-gate-checkable.

## Creatures & callbacks
- **Lexical Gremlin** — introduced L1 (antagonist), cameo L3 (BM25's blind spot), cornered L6 (synonyms near),
  **DEFEATED HERE** by contrastive learning. The arc's strongest payoff — lands as a *callback*, not a re-intro.
- **Sir Cosine** powers the contrastive objective (cosine similarity inside the loss).
- **the Council / Attention-Head** (NEW, one-tell-one-job): each token is a councillor casting attention weight
  over the others. Tell: a round table where each seat leans toward whom it heeds. Reused wherever attention recurs.
- **Curse-of-Dimensionality Wraith** cameo possible at `catch-anisotropy` (high-D pathology), but the Wraith's
  real payoff is L13 — keep it a one-line nod, don't duplicate.

## Acceptance (beat-coverage / facts)
Climbs exact: attention `softmax(QKᵀ/√d_k)V`, multi-head, positional encoding, the full block, contrastive loss.
The Gremlin payoff lands as a callback (not a re-intro). Anisotropy caveat present (P7, callback L2). Catchphrase
opens + closes; bridge to L7. All displayed numbers sourced from `data/l6-*.json` (facts-gate).
