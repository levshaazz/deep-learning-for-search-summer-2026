# L7.md — *Scouts and Judges* (beat sheet)

> **Lecture:** Bi-encoders (DPR, SBERT) · Cross-encoders & reranking · multi-stage pipelines.
> **Mode:** LOTR/quest. **Spine:** Rank. **Creatures:** **Sir Cosine** (bi-encoder scoring).
> **Slides:** `/slides/07` (EN, to build). **Book:** `/{lang}/book/07`.
> **Open:** *"Two kinds of crew: scouts who sweep fast, judges who deliberate slow."*
> **Close:** scouts + judges together; but too many to judge at scale (bridge → L8/L9).

## Spine position
Makes the L1 cascade *neural* (the Bridge): fast bi-encoder retrieval (Scouts) → accurate
cross-encoder reranking (Judges). The core architecture of modern neural search.

## Beats
| # | beat.id | kind | precision | widget | Slide (EN, terse) | Book (3-lang prose) |
|---|---------|------|-----------|--------|-------------------|---------------------|
| 0 | `hook-scouts-judges` | hook | — | — | `divider`: scouts vs judges | Cold open: speed vs accuracy — you can't have both everywhere |
| 1 | `problem-cant-score-all` | problem | — | — | `viz`: cross-encoder is O(N) — can't score every doc | The accuracy/scalability tension stated |
| 2 | `turn-two-architectures` | turn | — | — | `divider`: encode-separately vs encode-jointly | The two designs revealed |
| 3 | `climb-biencoder` | **climb** | **true** | **biencoder** | `arch`/`e2e`: encode q, d separately; dot/cosine; precompute d | Bi-encoder (DPR/SBERT), exact; why doc vectors precompute |
| 4 | `climb-crossencoder` | **climb** | **true** | **crossencoder** | `arch`: joint [q;d], full attention → one score | Cross-encoder, exact; accurate but uncacheable |
| 5 | `turn-multistage` | turn | — | — | `divider`: scouts retrieve, judges rerank | The multi-stage reveal (callback to L1 funnel) |
| 6 | `climb-cascade` | **climb** | **true** | **neural-cascade** *(reuse funnel)* | `funnel`: bi-encoder top-k → cross-encoder rerank | The cascade, exact k/latency; quality vs cost |
| 7 | `catch-rerank-depth` | catch | — | — | `quiz`: deeper rerank = more latency | The tradeoff trap; pick k for your SLA |
| 8 | `payoff-team` | payoff | — | — | `final` callback | Scouts+Judges win; but scouts still sweep linearly — bridge → ColBERT/hybrid (L8), ANN (L9) |

## Widgets & data
- `biencoder`/`crossencoder` ← `data/l6-encoders.json` (a q,d pair scored both ways);
  `neural-cascade` ← `data/l6-cascade.json` (stage k's + latencies) — **reuse `retrieve-rank-funnel`**.

## Creatures & callbacks
- **Sir Cosine** scores the Scouts. Callback to L1's funnel (now neural) and L6 (the encoders use the
  contrastively-trained space).

## Acceptance (beat-coverage)
Three climbs (bi, cross, cascade) exact; funnel reused (not redrawn); catchphrase opens+closes; bridges
to L8 and L9.
