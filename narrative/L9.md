# L9.md — *The Oracle* (beat sheet)

> **Lecture:** RAG fundamentals · chunking · query understanding & rewriting. **Mode:** sci-fi.
> **Spine:** Rank → Generate. **Creatures:** **Tokenosaurus** (cameo — chunking). **Slides:**
> `/slides/09` (EN, to build). **Book:** `/{lang}/book/09`. **Open:** *"The Ship has always found
> records. Today it learns to answer."* **Close:** the Oracle speaks — but is it telling the truth?
> (bridge → evaluation, L10).

## Spine position
Adds *Generate* on top of *Rank* (the Bridge gains a voice): retrieval-augmented generation, the
chunking that feeds it, and the query understanding that aims it.

## Beats
| # | beat.id | kind | precision | widget | Slide (EN, terse) | Book (3-lang prose) |
|---|---------|------|-----------|--------|-------------------|---------------------|
| 0 | `hook-oracle` | hook | — | — | `divider`: the Oracle | Cold open: the LLM hallucinates / is stale — it needs grounding |
| 1 | `problem-ungrounded` | problem | — | — | `viz`: confident wrong answer | Why a bare LLM can't be trusted as a search answer |
| 2 | `turn-rag` | turn | — | — | `divider`: retrieve → augment → generate | The RAG loop revealed |
| 3 | `climb-rag` | **climb** | **true** | **rag-pipeline** | `archflow` blueprint: chunk→embed→retrieve→stuff→generate | The pipeline, exact; what each stage contributes |
| 4 | `turn-chunking` | turn | — | — | `divider`: how you cut matters | Chunking revealed (Tokenosaurus cameo) |
| 5 | `climb-chunking` | **climb** | **true** | **chunking-demo** | `walkthrough`: size/overlap; retrieval quality | Chunk size/overlap tradeoffs, exact |
| 6 | `turn-query-rewrite` | turn | — | — | `divider`: hear the *real* question | Query understanding & rewriting revealed |
| 7 | `climb-queryrewrite` | **climb** | **true** | **query-rewrite** | `e2e`: HyDE / multi-query expansion | Rewriting, exact; why it lifts recall |
| 8 | `catch-garbage-in` | catch | — | — | `misconception`: bad retrieval → confident wrong answer | The Oracle lies if you feed it lies |
| 9 | `payoff-it-speaks` | payoff | — | — | `final` callback | The Oracle speaks; bridge → how do we *grade* it? (L10) |

## Widgets & data
- `rag-pipeline` ← `data/l9-rag.json` (the pipeline + a worked query→answer); `chunking-demo` ←
  `data/l9-chunking.json`; `query-rewrite` ← `data/l9-rewrite.json`.

## Creatures & callbacks
- **Tokenosaurus** (from L2) cameos at chunking. Callback to L8 (retrieval feeds the Oracle) and L1
  (the Iceberg of production now includes the generator).

## Acceptance (beat-coverage)
Three climbs (RAG, chunking, rewrite) exact; Tokenosaurus cameo only on the chunking turn; catchphrase
opens+closes; bridge to L10.
