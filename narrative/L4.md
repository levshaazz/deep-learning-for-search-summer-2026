# L4.md — *The Map of Meaning* (beat sheet)

> **Lecture:** Intro to DL for Search · Word embeddings · dimensionality reduction. **Mode:** LOTR/quest.
> **Spine:** Get Data (representation). **Creatures:** **Sir Cosine** (rides), **Lexical Gremlin**
> (being cornered). **Slides:** `/slides/04` (EN, to build). **Book:** `/{lang}/book/04`.
> **Open:** *"Remember the alien? Today it finally gets a place on the map."* (callback to First Contact)
> **Close:** words now have coordinates; bridge → but one word, many meanings (contextual, L5).

## Spine position
Builds the **meaning map** (Archives): dense vector representations of words, and the tools to *see*
them (dimensionality reduction). The leap from symbols to geometry.

## Beats
| # | beat.id | kind | precision | widget | Slide (EN, terse) | Book (3-lang prose) |
|---|---------|------|-----------|--------|-------------------|---------------------|
| 0 | `hook-map` | hook | — | — | `divider`: the Map of Meaning | Cold open: the alien gets coordinates (callback L2) |
| 1 | `problem-no-nearness` | problem | — | — | `viz`: one-hot/BM25 — every word equidistant | The Gremlin's home turf: symbols have no "near in meaning" |
| 2 | `turn-coordinates` | turn | — | — | `divider`: words → vectors (word2vec/GloVe) | The reveal: meaning becomes a *place* |
| 3 | `climb-word2vec` | **climb** | **true** | **embedding-space** | `e2e`: skip-gram objective; king−man+woman≈queen | The training objective + the analogy, exact; cosine measures nearness |
| 4 | `stakes-too-many-dims` | stakes | — | — | big-number: 300-d can't be seen | Zoom: you can't eyeball 300 dimensions |
| 5 | `turn-dimred` | turn | — | — | `divider`: fold the map (PCA → t-SNE/UMAP) | Dimensionality reduction reveal |
| 6 | `climb-dimred` | **climb** | **true** | **dimred-projection** | `formula`/`viz`: PCA variance; 2-D projection | Projection math, exact; what's preserved vs lost |
| 7 | `catch-tsne-lies` | catch | — | — | `misconception`: t-SNE distances aren't global | Don't over-read clusters/gaps — a common trap (P7) |
| 8 | `payoff-map-drawn` | payoff | — | — | `final` callback | The Map of Meaning drawn; Knights ride here; bridge → context (L5) |

## Widgets & data
- `embedding-space` ← `data/l4-embeddings.json` (word vectors, an analogy, cosine values);
  `dimred-projection` ← `data/l4-dimred.json` (high-d points + PCA components + 2-D coords).

## Creatures & callbacks
- **Sir Cosine** (from L2) is the ruler of this map — nearness = cosine. **Lexical Gremlin** is being
  cornered (synonyms now land near each other) → finished off in L5 by contrastive learning.

## Acceptance (beat-coverage)
Both climbs (word2vec, dimred) exact; t-SNE caveat present (anti-over-reading); catchphrase callback to
L2 at open; bridge to L5.
