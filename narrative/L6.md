# L6.md — *The Council of Attention* (beat sheet)

> **Lecture:** Dense & contextual embeddings · contrastive learning · Transformers & Attention.
> **Mode:** sci-fi. **Spine:** Get Data → Measure. **Creatures:** **Lexical Gremlin** (DEFEATED —
> the payoff), **Sir Cosine** (the contrastive objective). **Slides:** `/slides/06` (EN, to build).
> **Book:** `/{lang}/book/06`. **Open:** *"The crew has too many voices. Who do you listen to?"*
> **Close:** synonyms now collapse together — **the Gremlin is finally caged** (big callback to L1).

## Spine position
Upgrades the map from *static* to *contextual* (Archives) and learns a representation *for search*
(Measure). The Transformer is the engine; attention is how the crew decides who to heed.

## Beats
| # | beat.id | kind | precision | widget | Slide (EN, terse) | Book (3-lang prose) |
|---|---------|------|-----------|--------|-------------------|---------------------|
| 0 | `hook-council` | hook | — | — | `divider`: the Council of Attention | Cold open: a word's meaning depends on its neighbors |
| 1 | `problem-one-vector` | problem | — | — | `viz`: "bank" (river vs money) — one static vector | Static embeddings can't be in two minds; context is missing |
| 2 | `turn-attention` | turn | — | — | `divider`: let each token *attend* to others | The Transformer/attention reveal |
| 3 | `climb-attention` | **climb** | **true** | **attention-e2e** *(reuse existing e2e)* | `e2e`: softmax(QKᵀ/√d_k)V, synced numeric example | Scaled dot-product attention, exact; why √d_k; multi-head |
| 4 | `turn-contrastive` | turn | — | — | `divider`: train vectors *for retrieval* | How do we shape the space? Pull positives, push negatives |
| 5 | `climb-contrastive` | **climb** | **true** | **contrastive-space** | `e2e`/`formula`: InfoNCE / triplet; in-batch negatives | The objective, exact; cosine pulls synonyms together |
| 6 | `payoff-gremlin-caged` | payoff | — | — | `final` callback (the big one) | **Lexical Gremlin caged** (callback to L1) — synonyms now neighbors; bridge → bi/cross-encoders (L7) |

## Widgets & data
- `attention-e2e` ← `data/l5-attention.json` (Q,K,V, scores, softmax weights) — **reuse the existing
  e2e attention slide**; `contrastive-space` ← `data/l5-contrastive.json` (anchor/pos/neg, loss).

## Creatures & callbacks
- **Lexical Gremlin** payoff: introduced as the antagonist in L1, *defeated here* — the arc's strongest
  callback. **Sir Cosine** powers the contrastive objective (cosine similarity in the loss).

## Acceptance (beat-coverage)
Both climbs (attention, contrastive) exact; the Gremlin payoff lands as a callback (not a re-intro);
catchphrase opens+closes; bridge to L7.
