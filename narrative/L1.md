<!-- beats: hook-lost-record, problem-gremlin, stakes-scale, turn-cascade, climb-funnel, turn-system, climb-mlsd, catch-goodhart, payoff-forever -->
<!-- book: 01 -->

# L1.md — *The Lost Record* (beat sheet)

> **Lecture:** Search & IR · ML System Design. **Mode:** sci-fi (Project-Hail-Mary vibe).
> **Spine:** Rank-anchored, but sets up the whole loop. **Creatures:** **Lexical Gremlin** (intro),
> **Goodhart** (intro), **the Iceberg** (intro). **Slides:** `/slides/01` (EN, exists, 56).
> **Book:** `/{lang}/book/01`. **Open:** *"One record. Billions of others. Find it — then build the
> thing that finds it forever."* **Close:** the machine that keeps finding (bridge to representation).

## Spine position
Lays the **whole spine** out as a working system: IR = *find it* (the retrieve→rank cascade); ML
System Design = *the machine that keeps finding it*. Builds the Ship's first blueprint (the Bridge).

## Beats
| # | beat.id | kind | precision | widget | Slide (EN, terse) | Book (3-lang prose) |
|---|---------|------|-----------|--------|-------------------|---------------------|
| 0 | `hook-lost-record` | hook | — | — | `divider`: the lone operator hunting one record | Cold open: the Lost Record; why "just grep it" fails at scale |
| 1 | `problem-gremlin` | problem | — | — | `viz`: "couch" ≠ "sofa" — the wall | Enter the **Lexical Gremlin** (vocabulary mismatch); the naive match misses meaning (Furnas 1987) |
| 2 | `stakes-scale` | stakes | — | — | big-number: 10¹² pages, ~15% unseen queries/day | Zoom: scale makes brute force impossible |
| 3 | `turn-cascade` | turn | — | — | `divider`: retrieve → rank → re-rank | The funnel reveal: narrow billions → a handful |
| 4 | `climb-funnel` | **climb** | **true** | **retrieve-rank-funnel** | `funnel`: 10⁶ → 10² → 10 with stage costs | The cascade, exact stage counts; why each stage trades recall for precision |
| 5 | `turn-system` | turn | — | — | `divider`: "a demo isn't a system" → ML System Design | The pivot: shipping search ≠ a notebook |
| 6 | `climb-mlsd` | **climb** | **true** | `mlsd-blueprint` | `archflow` blueprint: offline/online, feature store, feedback loop | The production blueprint; **the Iceberg** (Sculley 2015 hidden debt) below the waterline |
| 7 | `catch-goodhart` | catch | — | — | `quiz`/`viz`: CTR as target; position bias γ≈0.94 | Enter **Goodhart** — a measure that became a target; clicks are *relative* (Joachims 2005) |
| 8 | `payoff-forever` | payoff | — | — | `final` callback | The machine that finds forever; bridge → *how do we even represent the records?* (L2) |

## Widgets & data
- `retrieve-rank-funnel` reads `data/l1-funnel.json` (stage counts/costs).
- `pos-bias-curve` / `mlsd-blueprint` read `data/l1-click-model.json` (γ≈0.94, top-1=32.3%, top-3=60.6%).
- `data/refs.json`: Furnas 1987, Broder 2002, Sculley 2015, Joachims 2005 (verified by check_claims).

## Creatures & callbacks
- **Lexical Gremlin** introduced here → **caged in L5** by contrastive learning (the payoff chain).
- **Goodhart** introduced → returns L3 (ranking metrics) and L10 (gaming the LLM-judge).
- **The Iceberg** introduced → returns L8/L9 (production is the 90% below water).

## Acceptance (beat-coverage)
Both climbs (funnel + blueprint) exact; creatures introduced with one-tell-one-job; catchphrase opens
+ closes; bridge to L2 explicit.
