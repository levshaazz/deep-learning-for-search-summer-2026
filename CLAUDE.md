# Deep Learning for Search — repo map

Course site (Astro) + offline lecture decks + trilingual Book + vanilla-JS SVG widgets, gated by 32 numbered CI gates (G1–G34, no G4/G5) + a facts-gate. **Goal when editing: touch the fewest files; load one unit, not the repo.** This map exists so you don't have to re-derive structure each session.

## Where things live
- `Lectures/<slug>/parts/*.html` — **the editable deck source** (one fragment per `<section class="slide">`, plus `00-head`/`zz-tail`). All 21 decks (L0–L20) are sharded.
  - `Lectures/NN-*.html` — the **offline standalone deck** (shipped artifact; loads `css/ js/ vendor/` relatively, runs over `file://`, 1920×1080, EN). It is **BUILD OUTPUT** (gitignored): `npm run build` reassembles it byte-identically from the fragments (`scripts/assemble-deck.mjs build`). **Do not edit it** — edit the fragments. (Decks 00–03 were accidentally *tracked* in `db11289` despite the ignore rule — gitignore does not apply to files already in the index — so every build dirtied them. Untracked 2026-08-15; if a deck ever shows up in `git status` again, untrack it rather than committing the artifact.)
- `content/book/<stem>/beats/*.js` — **the editable Book chapter source** (one fragment per beat, trilingual en/ru/tt). `content/book/lN.js` is the assembled chapter — **BUILD OUTPUT** (gitignored), reassembled byte-identically by `npm run build` (`scripts/assemble-chapter.mjs build`); **don't edit it**, edit the beat fragments. Glob-discovered via `src/lib/chapters.js` (adding a chapter needs no registration).
- `widgets/<name>/` — `logic.js` + `manifest.json` + `i18n.json` + `style.css` (+ `demo-slide.html`). Shared primitives: `widgets/_widget-base.js`, `_plot-util.js`, `_layout.js`. Auto-registered.
- `data/*.json` — **the single source of grounded numbers** (facts-gated). `data/course.json` = lecture catalog + logistics. Reproduced by `_research/gen_*.py`; `_research/data/*.json` are upstream generator artifacts.
- **⚠ TWO numberings coexist, deliberately.** The Aug-2026 reorder renumbered the *curriculum* (deck dirs `Lectures/NN-slug/`, book chapters `content/book/NN/` + the `id:` in their `00-head.js`, `data/course.json`, all prose refs "L7"). It did **NOT** renumber the *production* artifacts: `data/lNN-*.json`, `_research/gen_lNN.py`, and `Lectures/assets/img/LNN/` keep the numbers they were generated under — renaming them would break H3 byte-identity for zero benefit. So `data/l20-ru.json` legitimately feeds the deck now called `19-russian-search`, and `gen_l7_real.py` ("Scouts and Judges") feeds `10-bi-cross-encoders-reranking`. **This is not drift — do not "fix" it.** The old→new map (`_internal/` is gitignored, so it lives HERE, not in a plan file):
  `L0→0 L1→1 L2→2 L3→3 L4→5 L5→6 L6→7 L7→10 L8→12 L9→13 L10→15 L11→16 L12→20 L13→11 L14→17 L15→8 L16→18 L17→4 L18→14 L19→9 L20→19`.
  Read it as “the artifact named `l9-*` belongs to the deck now numbered 13”. Every row is confirmed by a seminar that consumes that data file (`seminars/`).
- `_research/check_claims.py` — **facts-gate** (data→generator provenance, deck displays == data, arithmetic). Run: `python3 _research/check_claims.py` (`--selftest` too).
- `narrative/chrome-ru.json` — **the closed dictionary of repeated deck chrome** (reveal button, misconception heading/kicker, payoff prefix), enforced by **G31** `check_chrome.py`; a new wording for one of these slots must land in the dictionary, not in the slide.
- `narrative/glossary-ru.json` — **the term canon of §7 as data**, enforced by **G33** `check_glossary.py`, which also fails if the JSON and the §7 table disagree: a new term decision must land in BOTH.
- `narrative/style-ru.md` — **the RU editorial style guide + approved glossary** (обращение «ты», decimal comma, term canon: чанк/эмбеддинг/би-энкодер/трудный негатив/развязка…). Any RU prose edit follows it; `scratchpad`-style fixers + the `_research/ru/check_style.py` linter enforce the mechanical half (`--selftest`, in CI since 2026-08-19; errors fail, warnings don't). The term canon is separately DATA in `narrative/glossary-ru.json`, read by **G33** `check_glossary.py`.
  **Frozen debt is counted, not promised.** `seminars/*.ipynb` sit OUTSIDE both perimeters while the owner runs the T4 kit — editing them would fork the repo from the copy in their hands. So both tools measure that debt every run and print a `DEBT(seminars/…)` line (no effect on exit code): 52 style findings across 7 notebooks, 17 banned term variants across 2. When a counter reaches zero it prints the opposite instruction — wire `seminars/*.ipynb` into the perimeter and delete `frozen_debt`. **Don't restore the deferral silently; either the counter or the perimeter must be live.**
  A caution the E-DEC narrowing bought: §2 (decimal comma) governs numbers-as-quantities, not «Шаг 1.2» / «| 2.3 |» / `avgdl = 3.0`. All 140 of the linter's decimal hits in the notebooks were that class — none was real. The narrowing is pinned by six self-test fixtures; widening it back needs a reason written next to them.
- `narrative/practice-standard.md` — **the floor under every seminar/lab notebook** (the practice analogue of the deck Definition of Done below). Derived by measuring 6 real notebooks: the exemplar reads its own results 27 times, the weak ones 1–3 — that ratio, not cell count, is what separates a live seminar from a script dump. Fixes the thresholds (≥80% of substantive code cells followed by markdown analysis · ≥8 traps across ≥4 of 6 types · 3–5 tasks each with an `assert` · two-layer `<details>` solutions), the runtime (Colab T4 free tier driven from VS Code — so no `#@param`, no `files.upload()`, no ipywidgets) and the markers (§15a) its gate reads. The gate is live: `python3 _research/check_practice.py` (**G24**, 10 HARD + 4 WARN, `--selftest`, `--measure "tmp/*.ipynb"` re-derives the thresholds). Check #1 — *did the notebook read its own result* — is the only one that separates the exemplar (81%) from the five below-bar notebooks (21–61%); three of those five clear the prose-share threshold, so the gate must never be reduced to word counting. All **7 seminars are written, run end-to-end and green** (556 cells, 57k words of RU prose, `seminars/` — see its README for the per-notebook table and what each one's *first run* disproved in its own already-written prose). **Write no seminar without the standard**, and follow §15a's authoring order: code → run → analyses, never the reverse.
- `stepik/` — Stepik release kit: `scripts/stepik-export.mjs` renders the Book's RU layer into 470 step HTML files + `manifest.json` (gitignored output), `stepik/graders/` holds the offline autograders; widgets embed via `src/pages/embed/[widget].astro` (`?lang=ru`).
- `scripts/export-pptx-extract.mjs` + `scripts/build_pptx.py` — **экспорт деков в PowerPoint для дизайнера**: браузер снимает геометрию каждого блока в design-space 1920×1080, сборщик кладёт её в pptx (текст остаётся текстом, формулы — исходным TeX, виджеты/иллюстрации — картинками, заметки — в notes; рядом эталонный рендер каждого слайда). Инструкция и фирменный стиль: `release/design-handoff.md`. Зависимость `python-pptx` живёт в gitignored `_research/.venv-pptx` — студенческий рантайм не трогает (H5).
- `release/` — `legal-review.md` (licences, AI-image terms, dataset/brand flags) + `video/` (recording pipeline + shooting scripts L0–L3).
- `_research/mascots.py` — **the locked character bible**: Séréga + the recurring cast (Tokenosaurus, Sir Cosine, Lexical Gremlin, Goodhart, the first-contact Alien…). One source of truth for each mascot's look so the brand stays consistent across lectures/sessions; `gen_images.py` imports `SEREGA` from it. Reuse this cast in new illustration briefs; add a NEW mascot only by locking an entry here first.
- `_research/check_images.py` — **image-gate** (brand/mascot): every lecture must feature Séréga in ≥40% of its plates AND in its hero (first) + final (last) plate, and GREEN may appear ONLY on his tübetey. Pure-Python (ast-reads `gen_images.py` briefs). Run: `python3 _research/check_images.py` (`--selftest` too). Exists because the mascot once silently faded out (5→3→0→0 plates across L5→L8).
- `_audit/*-gate.mjs` — the CI gates (+ `wbw-check.mjs`, `offline-deck.mjs`). `_audit/lib/gate-harness.mjs` = shared server/browser helpers.
- `src/` — Astro pages/layouts/i18n; `src/lib/{course,chapters,assignments}.js`. `scripts/` — `copy-static.mjs`, `assemble-deck.mjs`. `tokens/design-tokens.css` — colors. `narrative/L*.md` — beat sheets.

## Per-task recipes (touch only these)
- **Fix one slide:** edit the fragment `Lectures/<slug>/parts/<NN-slug>.html`, then `node scripts/assemble-deck.mjs build <slug>` (or `npm run build`) to reassemble `Lectures/<slug>.html`. Never edit the assembled `Lectures/NN-*.html` (it's gitignored build output and gets overwritten). Verify: `cd _audit && node wbw-check.mjs <slug>.html` (+ `node slide-viz-gate.mjs --strict`, whose curated per-figure targets sit on L6/L7) and `node offline-deck.mjs <slug>.html` for the `file://` guarantee.
- **Change one number:** edit `data/<file>.json` (the source); re-run its generator and prove H3 byte-identity with `bash _research/reproduce.sh`; `check_claims` enforces deck==data, Book==data, AND (coverage-guard `[G]`) that no NEW displayed number is un-gated. The number is also hand-typed prose at each display site — `git grep` it across the deck + Book to update all copies (a new display site must be gated or it HARD-fails the coverage-guard).
- **Add a widget:** create `widgets/<name>/{logic.js,manifest.json,i18n.json,style.css}` — auto-discovered by Book, Playground, and `widget-render-check`.
- **Add lecture L7:** `data/course.json` entry + the Book chapter (write `content/book/l7.js`, then `node scripts/assemble-chapter.mjs split l7` to shard it — the monolith is gitignored, the `beats/` fragments are tracked) + the deck under `Lectures/07-slug/parts/`. Registries are glob-driven — `BOOK_READY`, `check_claims DECKS`, `wbw-check`, `responsive-gate`, book route, beat/scroll/i18n gates all auto-discover. (Curated `slide-viz` `DECK_TARGETS`/`BOOK_TARGETS` are optional per-figure defect targets, not per-unit registration.) **Illustrations:** add the briefs to `_research/gen_images.py` `JOBS` reusing the `mascots.py` cast — Séréga must appear in ≥40% of plates AND in the hero + final plate (the image-gate enforces this; don't ship Séréga-less abstract plates). Generate with `python3 _research/gen_images.py L7`.
- **Add homework/seminar:** `src/lib/assignments.js` — content-first, ~0 plumbing (the reference model).

## Что сознательно НЕ гейтится (и почему — не переоткрывать)
- **Провенанс квалификаторов (план 4.5).** Замысел: пометка «игрушечный пример / относительные
  проценты / GPU-батч» обязана стоять на том же слайде, где показано число. Проверены три
  формулировки, все — театр: (1) «число совпало со значением из toy-файла» → 223 слайда, среди них
  √2 и параметр `b` BM25; (2) то же с уникальной принадлежностью значения одному файлу → 37, но
  ручная проверка первого же (`L18 · 06 Hook`) показала, что там числа **из статьи Günther 2024**,
  а совпадение случайно; (3) «слайд-разбор с числами обязан нести слово-оговорку» → 22 из 28, и это
  слайды вроде «BM25 на кошках и собаках», где корпус из трёх документов виден глазами. Причина
  общая: происхождение числа не выводится из его значения, потому что одни и те же величины
  приходят и из статей, и из игрушечных расчётов. Надёжный гейт возможен только если у claim'ов
  в `check_claims.py` появится ЯВНОЕ поле провенанса (515 записей, 14 реестров) — до тех пор
  проверки нет, и это лучше, чем 37 «нарушений», которые все научатся пролистывать.
- **«Акт закрывается слайдом-границей» (часть плана 4.8).** Правило есть в Definition of Done, но
  его не проходят эталонные деки 10 и 12 (7 актов из 8 и 4 из 5): акты закрываются содержательным
  слайдом, а не служебным ярлыком. Убрано из G34 после замера — см. коммит гейта.

## Hard constraints (do not break)
H1 decks are offline standalone (`file://`, zero network, 1920×1080, EN/RU). H2 Book is trilingual via i18n. H3 numbers come from `data/` + are reproducible/facts-gated (any data/generator change must leave `data/` JSON byte-identical: `check_claims` green + empty `git diff data/`; **prove with `bash _research/reproduce.sh`** — the full local toolchain is CPython 3.9 + vendored pylibs, spec frozen in `_research/requirements-repro.txt`). H4 the CI gates + facts-gate stay working (consolidate/strengthen, never weaken) — incl. the depth/asset/composition/readability gates **G17 `check_assets`** (every referenced/briefed illustration exists), **G18 `parity-gate`** (depth + no walls of text), **G19 `composition-gate`** (no frame overflow), **G20 `readability-gate`** (масштаб: no PROSE below the legibility floor — deck 11px / book 12px raw font), and **G13 `viz-probe`** which now HARD-fails a SEVERE label overlap (≥50% box cover — наложения), not just WARN; see the Definition of Done below. H5 no heavy runtime deps for students; widgets are vanilla-JS SVG.

## Verify before committing
`python3 _research/check_claims.py` (+`--selftest`) · `npm run build` (must be **204 pages**) · the relevant `_audit/` gate(s) · for an illustration/`gen_images.py` change: `python3 _research/check_images.py` (+`--selftest`) **and** `python3 _research/check_assets.py` (+`--selftest`) · for a Book/deck pairing change (new chapter, new beats, a widget mounted or removed): `python3 _research/check_pairing.py` (**G30**: slides/beats ≤ 4.0 and ratcheted per pair; every Book widget must stand on some slide — mount it or justify it in BOOK_ONLY) · for a seminar change: `python3 _research/check_practice.py` (+`--selftest`) and `python3 _research/check_notebooks.py` (**G29**: prose numbers must come from the notebook's own run dump `seminars/runs/` or `data/`; re-run the notebook, don't edit the dump) · for a deck change: `node _audit/mechanism-gate.mjs` (**G32**: mechanism density ≥12 % and no run of >20 slides without a derivation — both ratcheted) + `node _audit/parity-gate.mjs` + `node _audit/composition-gate.mjs` + `node _audit/readability-gate.mjs` (+`--selftest`; G20 also covers the Book) · for a `data/`/generator change: `bash _research/reproduce.sh` (byte-identity, H3). Deep context + the refactor plan/log: `_audit-report/` (`CONTEXT-ARCH-AUDIT.md`, `REFACTOR-LOG.md`).

## Definition of Done for a lecture — gates are the FLOOR, L10–L12 are the BAR
**Green gates mean "no regression in correctness" — they do NOT mean "good."** The bar is: *indistinguishable from L10–L12 in depth + polish, proven by LOOKING at the rendered slides.* A lecture is born to this bar, never rebuilt to it. Before calling a deck done, every box must be ticked:
- [ ] **Depth ≥ ~50 slides** (L10–L12 = 50–63), one idea per slide, full scaffolding (you-are-here · agenda · objectives · per act: divider → hook → problem → formal → climb-widget → worked-by-hand → real example → frontier → boundaries · synthesis · references). **Build FROM the reference deck's structure, not from scratch.** → `node _audit/parity-gate.mjs` (**G18**).
- [ ] **No walls of text** — median ≤ ~1150 visible chars/slide; use def-cards / formula-stages / cmp-tables / images, never paragraphs. → parity-gate (**G18**).
- [ ] **Illustrations actually generated** — every `gen_images.py` brief → a committed PNG, every referenced image exists on disk, made with the course's reference model **`nano-banana-pro`** (not gpt-image-1). → `python3 _research/check_assets.py` (**G17**) + brand `check_images.py` (**G12**).
- [ ] **Composition holds** — no slide content overflows/clips the 1920×1080 frame. → `node _audit/composition-gate.mjs` (**G19**) + `slide-viz-gate.mjs` (**G9**) for figures.
- [ ] **Readable — scale + no collisions** — масштаб: no PROSE authored below the floor (deck 11px / book 12px raw); наложения: no figure-label collision ≥50%. → `node _audit/readability-gate.mjs` (**G20**) + `node _audit/viz-probe-gate.mjs` (**G13**, severe overlap now HARD). Figures fit-scale by design; their legibility is G9/G13's, not a raw-font concern.
- [ ] **Auto-fit ≥ 0.65, and never below 0.62** — `legibility-gate` (**G22**) HARD-fails under **0.62** and WARNs in the **0.62–0.65** band, because a slide authored at 0.61 passes on macOS and fails on CI's Linux (it did, twice). Aim for the target, not the floor. Sub-16px labels INSIDE figures are ratcheted by `viz-probe` against `_audit/baselines/viz-tiny-text.json`: NEW or WORSE is HARD. **Both baselines may only shrink, and this is now mechanical, not a promise** — `--update-tiny-baseline` / `--update-baseline` REFUSE to write a re-freeze that adds an entry or worsens one, and `viz-probe` exits 1 if its baseline file is missing/corrupt (that file is the ratchet's arming pin: deleting it used to drop the whole rule back to WARN).
- [ ] **Agenda anchors verified by CLICKING** — `#/N` is a slide POSITION, not the number in `data-screen-label`; the two diverge the moment a lettered insert (`25a`) lands. Navigate each anchor in a browser and confirm it reaches a divider — `check_narrative` agrees with the engine only since its section regex was fixed to match `class="… slide …"`, not `class="slide"` exactly.
- [ ] **You LOOKED at it** — `node _audit/composition-gate.mjs --contact NN-slug.html` → review every screenshot in `_internal/contact-sheets/NN-slug/` against L10–L12. **Never declare done on gate output alone.**

Why this list exists: each item is a failure mode that *shipped with every legacy gate green* (L13 once shipped at 13 dense slides, image-less, with an overflowing ablation slide). Sessions have no memory — the bar lives HERE + in the gates (**G17/G18/G19/G20** + **G13**), never in anyone's head.
