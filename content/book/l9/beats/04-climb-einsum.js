    // THE ISOMORPHISM, and the reason this chapter is not a lecture about pictures. ncd-einsum holds the
    // SAME contraction in three panes at once — circuit · einsum subscript string · formula with a Σ — and
    // lights one index across all three. The rule falls out of the string itself: an index that appears on
    // the LEFT of the arrow and not on the RIGHT has been contracted — which is a cup in the diagram and a
    // Σ in the formula. Not an analogy: the same statement, three alphabets. It earns its place here because
    // it is the only figure in the Book that proves the diagram is not an illustration OF the computation —
    // it IS the computation, written down. Everything the chapter later claims about COST rests on that.
    // Structural, no grounded numbers (manifest data: []). 4 steps.
    { id: 'climb-einsum', kind: 'scrolly', widget: 'ncd-einsum' },
