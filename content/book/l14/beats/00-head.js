// content/book/l14.js — "The Artificer's Quill" (L14): advanced query rewriting & decomposition —
// HyDE, step-back, multi-query + RRF, decomposition, and the trainable rewriter (RRR). Deep-dive #2
// after the core L0–L12; it deepens L10's query understanding. Metaphor: Séréga is the Artificer, and
// the Quill rewrites the captain's rough question to close three gaps — vocabulary, specificity,
// compositionality. Two scrolly widgets anchor the climbs — hyde-embed and query-rewriter — each
// reading data/l14-rewrite.json (toy = stdlib-reproducible). PROSE carries the story QUALITATIVELY;
// every NUMBER is shown by the mounted widgets (so it stays a single source). EN canonical + RU
// (native) + TT. KaTeX uses \lt/\gt and never a literal < or > inside math.

export default {
  id: '14',
  catchphrase: "The Artificer's Quill",
  beats: [
