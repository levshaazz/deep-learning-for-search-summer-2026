    {
      id: 'depth-kv-cache', kind: 'prose',
      heading: { en: "The KV-cache: a shelf of finished rows", ru: "KV-кэш: полка готовых рядов", tt: "KV-кэш: әзер рәтләр киштәсе" },
      body: {
        en: [
          "Generation is one token per step — and a naive loom would re-measure the *entire* woven prefix at every step, as if seeing it for the first time. But look at what actually changes: the keys \\(K\\) and values \\(V\\) of past positions *don't*. Once a thread is woven, its \\(K\\) and \\(V\\) rows are fixed for good. So keep them — that is the **KV-cache**. Each new step computes \\(Q, K, V\\) for the *one* new token only; its fresh query then reads all the old keys and values straight off the shelf. Note what is *not* cached — the queries: each \\(q\\) is used once, on its own step, and discarded.",
          "The trade is compute bought with memory, and it is worth knowing which axis grows. The shelf holds a \\(K\\) row *and* a \\(V\\) row for every past position, in every layer, for every head — and it grows by one row with each token you emit, for as long as the generation runs. The step itself, meanwhile, is cheap: one query row against the stored keys, a single row of scores — nothing like the full square matrix the prefix once cost. And one scope note: the cache speeds up **inference only**. In training there is nothing to cache — teacher forcing already computes the whole prefix in one parallel pass.",
        ],
        ru: [
          "Генерация — по токену за шаг, и наивный станок на каждом шаге перемерял бы *весь* сотканный префикс заново, будто видит его впервые. Но посмотри, что на самом деле меняется: ключи \\(K\\) и значения \\(V\\) прошлых позиций — *не меняются*. Нить соткана — её строки \\(K\\) и \\(V\\) застыли навсегда. Так сохрани их — это и есть **KV-кэш**. Каждый новый шаг считает \\(Q, K, V\\) только для *одного* нового токена; его свежий запрос читает все старые ключи и значения прямо с полки. Заметь, что *не* кэшируется, — запросы: каждый \\(q\\) используется один раз, на своём шаге, и выбрасывается.",
          "Сделка — вычисления, купленные памятью, и стоит знать, какая ось растёт. Полка держит строку \\(K\\) *и* строку \\(V\\) для каждой прошлой позиции, в каждом слое, для каждой головы — и растёт на одну строку с каждым выданным токеном, пока идёт генерация. Сам же шаг дёшев: одна строка запроса против сохранённых ключей, одна строка оценок — ничего похожего на полную квадратную матрицу, в которую однажды обошёлся префикс. И одна оговорка о границах: кэш ускоряет **только инференс**. На обучении кэшировать нечего — teacher forcing и так считает весь префикс за один параллельный проход.",
        ],
        tt: [
          "Генерация — адымга бер token, һәм беркатлы станок һәр адымда тукылган префиксны *тулысынча* яңадан үлчәр иде, аны беренче тапкыр күргәндәй. Ләкин чынлыкта нәрсә үзгәрә — кара: узган позицияләрнең ачкычлары \\(K\\) һәм кыйммәтләре \\(V\\) — *үзгәрми*. Җеп тукылган икән — аның \\(K\\) һәм \\(V\\) юллары мәңгегә каткан. Шуңа аларны сакла — бу **KV-кэш**. Һәр яңа адым \\(Q, K, V\\)'ны бары *бер* яңа token өчен генә исәпли; аның яңа соравы иске ачкычлар белән кыйммәтләрне туп-туры киштәдән укый. Нәрсә кэшләнми икәненә игътибар ит — сораулар: һәр \\(q\\) бер тапкыр, үз адымында кулланыла да ташлана.",
          "Килешү — хәтер бәрабәренә сатып алынган исәпләү, һәм кайсы күчәр үскәнен белү мөһим. Киштә һәр узган позиция өчен \\(K\\) юлын *һәм* \\(V\\) юлын тота, һәр катламда, һәр баш өчен — һәм син чыгарган һәр token белән бер юлга үсә, генерация барганда туктамыйча. Ә адым үзе арзан: сакланган ачкычларга каршы бер сорау юлы, бер юл бәя — префикс бер заман түләгән тулы квадрат матрицага һич охшамаган. Һәм бер чик искәрмәсе: кэш **инференсны гына** тизләтә. Өйрәткәндә кэшләр нәрсә юк — teacher forcing болай да бөтен префиксны бер параллель үтештә исәпли.",
        ],
      },
    },
