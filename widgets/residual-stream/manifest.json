{
  "id": "residual-stream",
  "version": "1.0.0",
  "title": { "en": "The residual highway: x → x+attn → Norm → x+ffn → Norm", "ru": "Остаточная магистраль: x → x+attn → Norm → x+ffn → Norm", "tt": "Калдык юлы: x → x+attn → Norm → x+ffn → Norm" },
  "maxStep": 4,
  "data": ["l6-residual"],
  "i18n": "widgets/residual-stream/i18n.json",
  "export": "mountResidualStream",
  "protectedTerms": ["LayerNorm", "attn", "ffn"],
  "steps": [
    { "step": 0, "labelKey": "s0" },
    { "step": 1, "labelKey": "s1" },
    { "step": 2, "labelKey": "s2" },
    { "step": 3, "labelKey": "s3" },
    { "step": 4, "labelKey": "s4" }
  ],
  "mounts": { "book": { "driver": "scrollama" }, "slide": { "dataType": "viz", "driver": "deck-step" } }
}
