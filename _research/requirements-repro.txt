# requirements-repro.txt — the EXACT pinned toolchain that produces data/ + _research/data/
# byte-identically (hard constraint H3). Frozen from the vendored cache _research/data/.cache/pylibs/.
#
# The cache itself is gitignored (platform-specific .so binaries, macos-aarch64 / CPython 3.9) — THIS
# file is the tracked, durable spec so the toolchain survives cache loss and is precisely rebuildable.
# Install under CPython 3.9 into the cache dir (see `bash _research/reproduce.sh --setup`):
#     /usr/bin/python3 -m pip install --target _research/data/.cache/pylibs -r _research/requirements-repro.txt
# then `bash _research/reproduce.sh` to prove byte-identity. NOTE: byte-identity is tied to the platform
# (macos-aarch64) + CPython 3.9 + this exact tree; a different OS/BLAS may drift floats (re-baseline then).
joblib==1.5.3
llvmlite==0.43.0
numba==0.60.0
numpy==2.0.2
pynndescent==0.6.0
scikit-learn==1.6.1
scipy==1.13.1
threadpoolctl==3.6.0
tqdm==4.68.2
umap-learn==0.5.12
