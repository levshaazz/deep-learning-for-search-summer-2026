# L2-20 — granularity tradeoff (companion to the SVG triangle on slide 20)

Generated by `_research/gen_images.py` with hardened session-2 prompts
(PREAMBLE + ANTIPATTERN; SEREGA skipped — has_serega=False).

Wiring: appears as `.art-strip` under the SVG triangle on slide 20 of
`Lectures/02-nlp-tokenization-similarity.html`. The SVG (vocab/seq/OOV vertices)
remains the precise teaching artefact; this companion is the WBW metaphor.

Scene (verbatim):

  the granularity tradeoff drawn as a small juggler with three balls labelled
  'vocab', 'seq len', and 'OOV'; the juggler can only keep two in the air
  comfortably while the third drops; arrows show that pushing 'vocab' down
  inflates 'seq len' and vice versa. Hand-lettered labels exactly: 'vocab',
  'seq len', 'OOV'.

Regenerate:
  python3 _research/gen_images.py --force --only L2-20-tradeoff
